syntax = "proto3";
package stafiprotocol.stafihub.rate;

import "rate/rate.proto";
// this line is used by starport scaffolding # genesis/proto/import
import "gogoproto/gogo.proto";

option go_package = "github.com/stafiprotocol/stafihub/x/rate/types";

// GenesisState defines the rate module's genesis state.
message GenesisState {
    repeated ExchangeRate exchangeRateList = 1 [(gogoproto.nullable) = false];
  // this line is used by starport scaffolding # genesis/proto/state
}
